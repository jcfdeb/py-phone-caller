---
# Backwards-compatible entry point. The deployment now lives in
# deploy_py-phone-caller.yml, which reads its configuration from group_vars
# generated by assets/ansible/tools/toml_to_ansible_vars.py.
- name: Deploy py-phone-caller (compatibility wrapper)
  ansible.builtin.import_playbook: deploy_py-phone-caller.yml
